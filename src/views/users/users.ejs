<%- include('../partials/head.ejs',{styles: ['base', 'footer' , 'header' , 'index' ]}) %>
<body>
    <%- include('../partials/headerAdmin.ejs') %>

    <main>
        <% locals.users && users.forEach((user) => { %>
            <!-- TO-DO: aplicar estilos a esta vista -->
            <div style="color: black; background-color: red;">
                <a href="/users/ <%= user.id %>">
                <div>
                    <p>rol: <%= user.rol %></p>
                    <p>nombre: <%=user.name  %></p>
                </div>
                </a>
            </div>

        <% }); %>
    </main>

</body>
</html>